#include "greedy_player.h"
#include "board.h"



// int evaluateBoard(Board &board) {
//   for (int i = 0; i < ROWS; i++) {
//     for (int j = (1 - i % 2); j < COLS; j += 2) {
//       Pos pos = Pos(i, j);
//       if (!board.pieceboard.isEmpty(Pos pos)
//     }
//   }
// }

int GreedyPlayer::decideOnBoard(State state) {
  return 0;
}

